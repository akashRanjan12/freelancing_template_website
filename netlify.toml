[build]
  command = "npm run build"
  publish = ".next"

# The Next.js plugin is automatically installed by Netlify, 
# but declaring it ensures the correct adapter is always used.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
